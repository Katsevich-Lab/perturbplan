#' Launch the PerturbPlan Shiny App
#'
#' @return None
#' @export
# launch_app <- function() {
#   app_dir <- system.file("shiny", package = "perturbplan")
#   if (app_dir == "") {
#     stop("Could not find app directory. Try reinstalling the `perturbplan` package.", call. = FALSE)
#   }
#   shiny::runApp(app_dir)
# }
