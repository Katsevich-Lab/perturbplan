% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pilot_data_preprocessing.R
\name{obtain_gene_id}
\alias{obtain_gene_id}
\title{Obtain Gene ID ↔ Gene Name Table from Cell Ranger Output}
\usage{
obtain_gene_id(
  path_to_cellranger_output,
  id_path = "features/id",
  name_path = "features/name"
)
}
\arguments{
\item{path_to_cellranger_output}{Character. Path to an SRR-named folder
(e.g. \code{"SRR12345678"}).}

\item{id_path}{A string. Path within the HDF5 file to the gene IDs
dataset. Default is \code{"features/id"}.}

\item{name_path}{A string. Path within the HDF5 file to the gene names
dataset. Default is \code{"features/name"}.}
}
\value{
A \code{data.frame} with the columns:
\itemize{
\item \code{gene_id}: Ensembl gene ID.
\item \code{gene_name}: Gene symbol.
}
}
\description{
\code{obtain_gene_id()} reads a \code{molecule_info.h5} file generated by
\strong{Cell Ranger} (10x Genomics) and returns a tidy mapping between
Ensembl gene identifiers and gene symbols. The function extracts the
data stored in the HDF5 datasets \code{features/id} and \code{features/name}
(or alternative paths supplied via the arguments) and combines them
into a two-column \code{data.frame}.
}
\details{
The function relies on the \pkg{rhdf5} package for HDF5 I/O.
If the package is not installed, the function will throw an error.
}
